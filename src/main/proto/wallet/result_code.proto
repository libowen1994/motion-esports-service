syntax = "proto3";
 
option java_package = "one.motion.proto.wallet.model";

option java_multiple_files = true;

enum ResultCodeProto { //结果代码
    UNDIFINED = 0;
    SUCCESS = 200;
	FAIL = 400;
    UNAUTHORIZED = 401; //(401)未认证（签名错误）
    PARAMS_ERROR = 403; //(403)参数错误
    DUPLICATE_ERROR = 40301; //(40301)存在有重复对象
    SYS_ERROR = 405; //(405)系统错误
    NOT_FOUND = 404; //(404)接口不存在
    INTERNAL_SERVER_ERROR = 500;//(500)服务器内部错误
}